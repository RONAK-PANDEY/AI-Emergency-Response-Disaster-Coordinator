import 'leaflet/dist/leaflet.css';

import React from 'react';
import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet';
import L from 'leaflet';

// Helper to create a simple colored pin icon using divIcon (no external image assets needed)
function createColorIcon(color) {
  return L.divIcon({
    className: 'custom-div-icon',
    html: `
      <div style="
        background-color: ${color};
        width: 24px;
        height: 24px;
        border-radius: 50% 50% 50% 0;
        transform: rotate(-45deg);
        border: 2px solid #ffffff;
        box-shadow: 0 0 4px rgba(0,0,0,0.5);
      "></div>
    `,
    iconSize: [24, 24],
    iconAnchor: [12, 24],
    popupAnchor: [0, -24],
  });
}

const fireIcon = createColorIcon('#e63946');   // red
const floodIcon = createColorIcon('#1d70ff');  // blue
const accidentIcon = createColorIcon('#f4a300'); // orange/yellow

export default function PunjabMap() {
  return (
    <MapContainer
      center={[30.9, 75.8]}
      zoom={7}
      style={{ height: '100vh', width: '100vw' }}
    >
      <TileLayer
        url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
        attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
      />

      <Marker position={[30.901, 75.857]} icon={fireIcon}>
        <Popup>
          <strong>🔥 Fire Reported</strong>
          <br />
          Near Ludhiana city center.
        </Popup>
      </Marker>

      <Marker position={[30.94, 75.83]} icon={floodIcon}>
        <Popup>
          <strong>🌊 Flood Alert</strong>
          <br />
          Low-lying area north of Ludhiana.
        </Popup>
      </Marker>

      <Marker position={[30.87, 75.90]} icon={accidentIcon}>
        <Popup>
          <strong>🚧 Accident Reported</strong>
          <br />
          Road incident southeast of Ludhiana.
        </Popup>
      </Marker>
    </MapContainer>
  );
}